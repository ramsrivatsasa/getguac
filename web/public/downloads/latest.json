{
  "version": "v0.4.2",
  "name": "v0.4.2 — Real font fix",
  "downloadUrl": "https://getguac.app/downloads/v0.4.2/app-arm64-v8a-release.apk",
  "releaseNotes": "• Fixed the app fonts for real — the custom Bricolage + Jakarta type now actually renders on Android instead of falling back to the system font. (Switched to static font files, which the Android engine renders reliably.)\n• This build shows a small font-check strip on the Home screen; it will be removed in the next update once confirmed."
}
